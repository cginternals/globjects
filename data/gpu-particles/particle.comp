#version 330

uniform float delta; // time delta

layout (std430, binding = 0) buffer particles
{
	vec3 position[];
};

void main()
{
	uint i = gl_GlobalInvocationID.x;
	position[i] += vec3(1.0, 0.0, 0.0) * delta * 1000.1;
}
