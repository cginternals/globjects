#version 430

layout (local_size_x = 16) in;
//uniform float delta; // time delta

layout (std430, binding = 0) buffer Positions
{
	vec4 positions[];
};

void main()
{
	uint gID = gl_GlobalInvocationID.x;

	positions[gID] *= vec4(vec3(0.999), 1.0);
}
